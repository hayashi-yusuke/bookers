<%= form_with model: @book do |f| %>

  <p>
    Title: 
    <%= f.text_field :title %>
  </p>
  <p>
    Body: 
    <%= f.text_area :body %>
  </p>

  <%= link_to "Edit", edit_book_path(@book) %> |
  <%= link_to "Back", books_path %>
<% end %>